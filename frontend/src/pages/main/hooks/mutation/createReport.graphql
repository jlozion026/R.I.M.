mutation createOneReport($data: CustomReportCreateInput!){
  createOneReport(data: $data){
    report_id
    location
    description
    incident{
        date_started
        date_ended
      }
  }
}
